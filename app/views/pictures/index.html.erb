<h1>最新投稿！！</h1>

<table>
  <tr>
    <th>投稿者さん</th>
    <th>画像</th>
    <th>コメント</th>
  </tr>
  <% @pictures.each do |picture| %>
    <tr>
      <td><%= picture.user.name %></td>
      <td><%= image_tag(picture.image.url) if picture.image && picture.image.url %></td>
      <td><%= picture.content %></td>
      <td><%= link_to '確認', picture_path(picture.id) %></td>
      <% if picture.user_id == current_user.id %>
        <td><%= link_to '書き直し', edit_picture_path(picture.id) %></td>
        <td><%= link_to '消す', picture_path(picture.id), method: :delete, data: {confirm: '本当に消して良かですか？(>_<)'} %></td>
      <% end %>
    </tr>
  <% end %>
</table>
<br>
<%= link_to "あなたの今を共有しよう！", new_picture_path %>
